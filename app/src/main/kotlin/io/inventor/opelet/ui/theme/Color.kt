package io.inventor.opelet.ui.theme

import androidx.compose.ui.graphics.Color

// Light mode
val LightBackground = Color(0xFFFFFFFF)
val LightSurface = Color(0xFFF0F0F0)
val LightOnBackground = Color(0xFF000000)
val LightOnSurface = Color(0xFF000000)
val LightBorder = Color(0xFF000000)

// Dark mode
val DarkBackground = Color(0xFF1A1A1A)
val DarkSurface = Color(0xFF2A2A2A)
val DarkOnBackground = Color(0xFFFFFFFF)
val DarkOnSurface = Color(0xFFFFFFFF)
val DarkBorder = Color(0xFFFFFFFF)

// Status — no color differentiation per spec. These use opacity/weight instead.
val Muted = Color(0xFF888888)
